/**
 * Collectors are used to collect data from the music players. They are
 * used by Observers to collect data and emit events as well as by the
 * collector event handler to collect data when the background script or popup
 * requests it.
 */
