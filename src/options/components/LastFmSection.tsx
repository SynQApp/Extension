import { Button } from '@synq/ui';

import { OptionsSection } from './OptionsSection';

export const LastFmSection = () => {
  return (
    <OptionsSection title="LastFM">
      <Button>Connect to LastFM</Button>
    </OptionsSection>
  );
};
